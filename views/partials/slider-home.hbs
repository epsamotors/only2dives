<div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
  <ol class="carousel-indicators">
    <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
    <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
    <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
  </ol>
  <div class="carousel-inner">
    <div class="carousel-item active">
      <img class="d-block w-100" id="sliderOne" alt="First slide">
    </div>
    <div class="carousel-item">
      <img class="d-block w-100" id="sliderTwo" alt="Second slide">
    </div>
    <div class="carousel-item">
      <img class="d-block w-100" id="sliderThree" alt="Third slide">
    </div>
  </div>
  <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>

<script>
  fetch('/slider')
    .then(res => res.json())
    .then(data => {

      var sliderImages = [
        {
          id: '',
          url: ''
        }
      ];

      const slider = '';

      data.slider.forEach(sliders => {
        sliderImages.push({
          id: sliders.SLIDER_ID,
          url: sliders.SLIDER_URL,
        });
      });

      var eliminado = sliderImages.shift();//Eliminamos el primer objeto del array

      let sliderOne = document.getElementById("sliderOne");
      let sliderTwo = document.getElementById("sliderTwo");
      let sliderThree = document.getElementById("sliderThree");

      sliderOne.src = sliderImages[0].url;
      sliderTwo.src = sliderImages[1].url;
      sliderThree.src = sliderImages[2].url;

    });
</script>